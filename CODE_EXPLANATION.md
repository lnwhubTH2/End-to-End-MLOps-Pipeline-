# คำอธิบายการทำงานของโค้ดแต่ละส่วนเชิงลึก (Code Explanation) 🔬

เอกสารฉบับนี้จัดทำขึ้นเพื่อเจาะลึก อธิบายตรรกะ และวิเคราะห์เบื้องหลังการทำงาน (Under the hood) ของแต่ละองค์ประกอบในโปรเจกต์ MLOps นี้

---

## 1. ภาคการสร้างและจัดการโมเดล (Continuous Training)

### 🧠 `train.py` (โรงเรียนฝึกสอน AI)
โค้ดนี้เปรียบเสมือนขั้นตอนการเข้าค่ายเก็บตัวฝึกซ้อมของหุ่นยนต์ AI ของเรา
* **การเตรียมข้อมูล (Data Preparation):** เริ่มต้นจากการดึงไฟล์ `data/iris_new.csv` ขึ้นมาด้วย `pandas` ระบบจะนำคอลัมน์ชื่อ `target` (เฉลยข้อสอบ) ไปเก็บไว้ในตระกร้า `y` ส่วนสัดส่วนของดอกไม้ (ความกว้าง/ความยาว) ไปใส่ในตระกร้าข้อสอบ `X`
* **การฝึกทักษะ (Model Training):** นำอัลกอริทึม `DecisionTreeClassifier` (ต้นไม้ตัดสินใจ) มาบังคับเรียนรู้หาจุดเชื่อมโยงระหว่างตระกร้า `X` ไปสู่ `y`
* **เซฟสมองกล (Exporting Model):** สุดท้าย โค้ดจะใช้เครื่องมือ `joblib` เพื่อบีบอัดสมองที่ผ่านการเรียนรู้แล้ว ออกมาเป็นไฟล์ `challenger_model.pkl` ไว้ใช้ในงานถัดไป

### ⚖️ `evaluate.py` (กรรมการสอบและตัดสิน)
หลังจากเทรนโมเดลเสร็จ เราจะไม่สุ่มสี่สุ่มห้านำไปใช้งานจริงทันที สคริปต์นี้จะทำหน้าที่เป็นปราการด่านสุดท้าย:
* ระบบจะตั้งเกณฑ์คะแนนขั้นต่ำ (Champion Baseline) เอาไว้ที่ `0.80` (หรือแม่น 80%)
* มันจะปลุกโมเดลตัวใหม่ (Challenger) ขึ้นมาให้ทำข้อสอบชุดเดิม (เพื่อเช็คความจำและประสิทธิภาพเบื้องต้น) หากโมเดลทำคะแนนความแม่นยำ (`accuracy_score`) ได้ **มากกว่า 80%** สคริปต์จะใช้รหัสการเคลียร์ด้วย `sys.exit(0)` ซึ่งแปลว่าให้ไปต่อ
* แต่หากโมเดลใหม่ **โง่กว่าเดิมหรือพัง** มันจะหยุดระบบดื้อๆ ด้วย `sys.exit(1)` เพื่อป้องกันหุ่นยนต์ตัวอื่น (GitHub Actions) เอาโมเดลแย่ๆ ไปใช้

---

## 2. ภาคเปิดหน้าต่างให้บริการ (Model Deployment)

### 🔌 `app/main.py` (เคาน์เตอร์บริการรับ-ส่งข้อมูล)
นี่คือแอปพลิเคชัน Backend ที่ถูกเขียนขึ้นเพื่อปูหน้าต่างให้โปรแกรมเมอร์คนอื่นมาคุยกับโมเดลเราได้ (ผ่านไลบรารีชื่อ `FastAPI`)
* **Pydantic Validation:** โค้ดจะใช้ `class IrisFeature` มาคุมกำเนิดข้อมูลขาเข้า บังคับว่าทุกคนที่ส่งรีเควสต์เข้ามาหาเราผ่าน JSON **ต้อง** มีตัวแปรตัวเลข 4 ค่าเป๊ะๆ เท่านั้น หากส่งขาดหรือส่งคำพูดด่าทอมา ไวรัสก็เจาะเข้าไม่ได้และจะเด้ง 422 Unprocessable Entity
* **The Brain Integration:** เมื่อเปิด Server ปุ๊บ มันจะหาและโหลดไฟล์ `challenger_model.pkl` มารอในแรม หากหาไฟล์นี้ไม่เจอ โค้ดมีการทำระบบแถสีข้าง (Mock mode) ส่งค่าจำลองกลับไปแทนการพัง
* **Prediction Action:** หากข้อมูลถูกต้อง มันจะชุบตัว JSON ให้เป็น DataFrame แล้วยัดเข้าโมเดลเพื่อฟันธง แถมยังตีกลับค่าความมั่นใจที่หาได้จาก `model.predict_proba` แนบไปให้ผู้ใช้อุ่นใจด้วย

### 🕵️‍♂️ `tests/test_app.py` (สายตรวจคุณภาพ)
ไม่ต้องรอให้แอปพังถึงจะรู้ โค้ดส่วนนี้จะจำลองตัวมันเองเป็นผู้ใช้งาน:
* ด้วยฟีเจอร์ `TestClient` มันจะโหลดระบบทั้งหมดขึ้นมาจำลอง แล้วยิงข้อมูล JSON เข้าไปสุ่มถามที่ URL `/predict`
* จบด้วยการตั้งยาม (ใช้คำลั่ง `assert` ของ Python) เพื่อตรวจค้นตั๋ว หากการเสิร์ฟสำเร็จ (Status == 200) และคำตอบที่ได้มามีทั้ง `prediction_class` และ `confidence` แถมยังเป็นชนิดข้อมูลตัวแปรเป๊ะตามต้องการ มันก็จะตีตรา **"ผ่าน!"**

---

## 3. ภาคคนงานเงานอกระบบ (Infrastructure & CI/CD)

### 📦 `Dockerfile` (สูตรปั๊มแผ่น ซีดีรอมยุคใหม่)
เครื่องมือที่ทำให้ไม่ต้องเถียงกันว่า "ก็ตอนรันบนเครื่องคอมเรายังรันได้อยู่เลย!"
* นี่คือพิมพ์เขียว (Blueprint) ที่ระบุว่าเราต้องการเครื่อง Linux เปล่าๆ ทับด้วย Python 3.9
* สั่งให้มันโหลด Library (`requirements.txt`) ที่ถูกต้อง
* จากนั้นเทหน้าตัก ย้ายโค้ดทั้งหมด (ไฟล์โมเดล `.pkl` เอย, โฟลเดอร์ `app` เอย) เข้าไปล็อกเป้าไว้ที่โฟลเดอร์ `/app` โดยรันเซิร์ฟเวอร์ `uvicorn` เอาไว้สแตนด์บาย นี่คือสิ่งที่ทำให้โมเดลเราพร้อมแชร์ให้เพื่อนๆ รันบนโน้ตบุ๊กพวกเขาได้ลื่นปรื้ดเหมือนเครื่องเรา

### 🤖 `.github/workflows/ct_pipeline.yml` (หุ่นยนต์คุมงานสาย Data)
โฟกัสหน้าที่ไปที่ฝั่งข้อมูลล้วนๆ (Continuous Training)
* **Trigger:** จะตื่นเมื่อมีการแก้ไขไฟล์ `data/`, `train.py`, `evaluate.py`
* **Automated Process:** เทรนโมเดลใหม่ (`train.py`) -> วัดผลให้ตีกันเองแข่งกับแชมป์เก่า (`evaluate.py`)

### 🚀 `.github/workflows/ci_cd_pipeline.yml` (หุ่นยนต์คุมงานสาย App/Code)
โฟกัสที่องค์ประกอบฝั่งแอปพลิเคชันและการเปิดให้บริการ (CI/CD)
* **Trigger:** จะตื่นก็ต่อเมื่อมีการแก้ไขโค้ดของแอปอย่าง `app/`, `tests/` หรือ `Dockerfile` 
* **Automated Process:** รัน Unit Test ข้อสอบยืนยันพฤติกรรม API -> ประกอบร่าง Docker Image เป็นอันจบลูป
* หากมีสเต็ปไหนของการทำงานล้มเหลว ระบบการส่งมอบโค้ดทั้งหมดจะหยุดชะงักทันทีครับ
